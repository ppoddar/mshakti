<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- a single table of content item is a button that when clicked takes user -->
    <!-- to a specific page -->

    <Button
        android:id="@+id/index_item_translated_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="this is a list view button"
        android:layout_marginLeft="@dimen/activity_horizontal_margin"
        android:background="?android:attr/selectableItemBackground"

        android:textColor="@android:color/holo_blue_dark">

    </Button>

    <Button
        android:id="@+id/index_item_original_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/index_item_translated_button"
        android:layout_marginTop="-30dp"
        android:layout_alignLeft="@id/index_item_translated_button"

        android:text="this is a original text"
        android:background="?android:attr/selectableItemBackground"

        android:textColor="@android:color/black"/>

</RelativeLayout>