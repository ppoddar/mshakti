<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.shakti">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/shakti_profile"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activity.FrontPageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.PageActivity"
            android:parentActivityName=".activity.FrontPageActivity">
        </activity>
        <activity android:name=".activity.IndexPageActivity"
            android:parentActivityName=".activity.FrontPageActivity">
        </activity>
        <activity android:name=".activity.AboutActivity"
            android:parentActivityName=".activity.FrontPageActivity">
        </activity>

    </application>

</manifest>
